# Counter-App---React
A simple React Counter App built using the useState hook. It includes increment, decrement, and reset functionality with dynamic color updates based on the counter value. This project demonstrates basic state management and event handling in React.
